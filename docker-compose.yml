services:
  backend:
    build: ./backend
    ports:
      - "4141:4141"
    env_file:
      - ./backend/.env
    environment:
      - FLASK_ENV=development

  frontend:
    build: ./frontend
    ports:
      - "4200:80"
    depends_on:
      - backend